specVersion: "v1beta"
serviceId: "bmi_calculator"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bmi_calculator-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
